{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TODO.COM</h1>
	<p> <a href="{{ path('todo_new') }}">Ajouter une tache</a></p>
                
	<p> A faire: </p>
	<ul class="disc" > 
	 {% for entity in entities %}
     <li>  
	 <a href="{{ path('todo_show', { 'id': entity.id }) }}">{{ entity.name }}</a> </li>
     </li>  
	 {% endfor %}
	</ul>
    <i>CopyRight...</i>
    {% endblock %}